CREATE TABLE IF not  exists STUDENT(
   name  VARCHAR(50),
   age   VARCHAR(50),
   grade VARCHAR(3)
);

CREATE TABLE IF not exists STUDENT2(
   id identity,
   name VARCHAR(50),
   age VARCHAR(50),
   grade VARCHAR(3)
);